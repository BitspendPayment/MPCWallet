# OpenTofu state and outputs (contains account-specific IDs)
tofu-outputs.json
terraform.tfvars.json
terraform.tfstate
terraform.tfstate.backup
.terraform/
backend.tf
modules/enclave/.signing/

# GitHub Release download cache (EIF, supervisor binary, pcr.json).
# Re-downloaded on every apply; the S3 etag (filemd5) decides propagation.
modules/enclave/.artifacts/

# Deploy-time env overrides — contains the plaintext FCM service-account
# JSON (and any other operator-provided env values). Holds real secrets.
env_values.auto.tfvars.json
