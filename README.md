# Dart Version

This directory contains the Dart implementation of the MPCWallet project.
It mirrors the functionality of the Go `thresholdcore` package, providing
threshold cryptography capabilities in Dart.

For detailed information about the `threshold` package itself, please refer to:
`dart/threshold/README.md`

